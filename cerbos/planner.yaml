apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: planner
  version: default
  rules:
    - actions:
        - read
      roles:
        - Student
        - Student
      effect: EFFECT_ALLOW

    - actions:
        - write
      roles:
        - Instructor
      effect: EFFECT_ALLOW
