apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: planner
  version: default
  rules:
    - actions:
        - read
      roles:
        - Student
      effect: EFFECT_ALLOW

    - actions:
        - write
      roles:
        - Instructor
        - Student
      effect: EFFECT_ALLOW
