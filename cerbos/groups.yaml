apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: groups
  version: default
  rules:
    - actions:
        - write
      roles:
        - Instructor
      effect: EFFECT_ALLOW

    - actions:
        - read
      roles:
        - Instructor
      effect: EFFECT_ALLOW
